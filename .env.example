# Neon PostgreSQL connection string (injected automatically by Vercel Neon Integration)
DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require"

# Authorization secret token for accessing orders list admin route (GET /api/orders)
# Headers requirement: Authorization: Bearer your-admin-token-here
ADMIN_TOKEN="your-secret-admin-token"
